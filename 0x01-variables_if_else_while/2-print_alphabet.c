#include <stdio.h>

/**
 * main -> 
 * and
 * Return : always 0
*/
int main(void)
{
	int ch;
	for(ch=97; ch<=122; ch++)
	{
		putchar("ch");
	}
	putchar("10");
srand(time(0));
n = rand() - RAND_MAX / 2;
/* your code goes there */
return (0);
}
