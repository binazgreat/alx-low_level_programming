#include "main.h"
/**
 * _isalpha - checks for alphabetical character
 * @c: a character to bechecked on
 * Return: return 1 or 0 depending on condition
 */
int _isalpha(int c)
{
	((c >= 'a' && c <= 'z') || (c >= 'A' c <= 'Z'));
}
